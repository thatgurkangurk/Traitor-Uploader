# Traitor Uploader

Uploads assets to an uploader account to be loaded in-game.

## Why?

Because Roblox moderation is too aggressive and bans anything with scripts.

### warning

due to some ~~shenanigans~~ big brain workarounds regarding `@libsql/client`, please add **ALL** dependencies as dev deps

![image describing the workaround. basically, we install @libsql/client in a later step so it doesn't get bundled](./images/libsql_workaround.png)